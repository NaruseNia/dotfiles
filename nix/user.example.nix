{
  # Copy this file to `user.nix` and fill in your details.
  # `user.nix` is listed in .gitignore so your email never gets committed.

  username     = "your-unix-username";
  fullName     = "Your Name";
  email        = "you@example.com";
  hostname     = "mac";              # label for darwinConfigurations.<hostname>
  darwinSystem = "aarch64-darwin";   # or "x86_64-darwin"
  linuxSystem  = "x86_64-linux";     # or "aarch64-linux"
}
